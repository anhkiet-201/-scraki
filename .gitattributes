# Enforce LF line endings for all shell scripts bundled as Flutter assets.
# Prevents Windows Git (core.autocrlf=true) from converting LF -> CRLF,
# which breaks Android's /system/bin/sh shebang parsing.
assets/server/aki_remote text eol=lf
